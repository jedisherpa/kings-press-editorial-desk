{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local-editorial-desk",
  "description": "Allows the main King’s Press desktop webview to use local-first setup commands.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": ["core:default", "desktop-setup"]
}
